@namespace TabBlazor
@if (!RenderForm)
{
    @ChildContent
}
else
{
    <EditForm EditContext="@EditContext" OnValidSubmit="HandleValidSubmit" @attributes="UnknownParameters">
        @ChildContent
    </EditForm>
}