@page "/docs/progress"

    <Page>
        <DocsExample Title="Progress" ComponentType="typeof(Progress)">

            <CodeSnippet Component="@typeof(ProgressesBasic)" Title="Basic"/>

            <CodeSnippet Component="@typeof(ProgressesSize)" Title="Size"/>

            <CodeSnippet Component="@typeof(ProgressesIndeterminate)" Title="Indeterminate"/>

        </DocsExample>
    </Page>
   
