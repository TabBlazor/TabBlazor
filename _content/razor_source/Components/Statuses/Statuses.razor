@page "/docs/status"

<Page>
    <DocsExample Title="Status" ComponentType="typeof(Status)">

        <CodeSnippet ClassName="@typeof(Basic).ToString()" Title="Basic">
            <Example>
                <Basic />
            </Example>
        </CodeSnippet>

           <CodeSnippet ClassName="@typeof(WithDots).ToString()" Title="With Dots">
            <Example>
                <WithDots />
            </Example>
        </CodeSnippet>

          <CodeSnippet ClassName="@typeof(LiteStatus).ToString()" Title="Lite Status">
            <Example>
                <LiteStatus />
            </Example>
        </CodeSnippet>

        <CodeSnippet ClassName="@typeof(StatusDots).ToString()" Title="Status Dots">
            <Example>
                <StatusDots />
            </Example>
        </CodeSnippet>

            <CodeSnippet ClassName="@typeof(StatusIndicators).ToString()" Title="Status Indicators">
            <Example>
                <StatusIndicators />
            </Example>
        </CodeSnippet>

    </DocsExample>
</Page>
