@page "/docs/badges"


<Page>
    <DocsExample Title="Badges" ComponentType="typeof(Badge)">
        <CodeSnippet ClassName="@typeof(BadgesBasic).ToString()" Title="Basic Setup">
            <Example>
                <BadgesBasic />
            </Example>
        </CodeSnippet>

        <CodeSnippet ClassName="@typeof(BadgesLight).ToString()" Title="Light">
            <Example>
                <BadgesLight />
            </Example>
        </CodeSnippet>

        <CodeSnippet ClassName="@typeof(BadgesOutline).ToString()" Title="Outline">
            <Example>
                <BadgesOutline />
            </Example>
        </CodeSnippet>

        <CodeSnippet ClassName="@typeof(BadgesSize).ToString()" Title="Size">
            <Example>
                <BadgesSize />
            </Example>
        </CodeSnippet>

        <CodeSnippet ClassName="@typeof(BadgesPill).ToString()" Title="Shape">
            <Example>
                <BadgesPill />
            </Example>
        </CodeSnippet>

        <CodeSnippet ClassName="@typeof(BadgesEmpty).ToString()" Title="Empty">
            <Example>
                <BadgesEmpty />
            </Example>
        </CodeSnippet>

        <CodeSnippet ClassName="@typeof(BadgesNotification).ToString()" Title="Notification">
            <Example>
                <BadgesNotification />
            </Example>
        </CodeSnippet>

        <CodeSnippet ClassName="@typeof(BadgesBlink).ToString()" Title="Blink">
            <Example>
                <BadgesBlink />
            </Example>
        </CodeSnippet>

        <CodeSnippet ClassName="@typeof(BadgesList).ToString()" Title="Badge list">
            <Example>
                <BadgesList />
            </Example>
        </CodeSnippet>

    </DocsExample>
</Page>

