@page "/docs/avatars"

<Page>
    <DocsExample Title="Avatars" ComponentType="typeof(Avatar)">
        <CodeSnippet ClassName="@typeof(AvatarsBasic).ToString()" Title="Basic Setup">
            <Description>
                Basic avatar
            </Description>
            <Example>
                <AvatarsBasic />
            </Example>
        </CodeSnippet>

        <CodeSnippet ClassName="@typeof(AvatarsSize).ToString()" Title="Size">
            <Description>
                Size the avatar according to your needs
            </Description>
            <Example>
                <AvatarsSize />
            </Example>
        </CodeSnippet>

        <CodeSnippet ClassName="@typeof(AvatarsRounded).ToString()" Title="Rounded">
            <Description>
                Rounded setting
            </Description>
            <Example>
                <AvatarsRounded />
            </Example>
        </CodeSnippet>

        <CodeSnippet ClassName="@typeof(AvatarsStatus).ToString()" Title="Status">
            <Description>
                Set a status icon
            </Description>
            <Example>
                <AvatarsStatus />
            </Example>
        </CodeSnippet>

        <CodeSnippet ClassName="@typeof(AvatarsList).ToString()" Title="Avatars List">
            <Description>
                Wrap the avatars in a list. Set stacked if they should be stacked
            </Description>
            <Example>
                <AvatarsList />
            </Example>
        </CodeSnippet>

    </DocsExample>
</Page>