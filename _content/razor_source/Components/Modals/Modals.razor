@page "/docs/modals"

<Page>
    <DocsExample Title="Modals" ComponentType="typeof(ModalModel)">

        <CodeSnippet ClassName="@typeof(ModalsBasic).ToString()" Title="Basic">
            <Example>
                <ModalsBasic />
            </Example>
        </CodeSnippet>

        <CodeSnippet ClassName="@typeof(ModalsOptions).ToString()" Title="Options">
            <Example>
                <ModalsOptions />
            </Example>
        </CodeSnippet>

          <CodeSnippet ClassName="@typeof(BuiltInModals).ToString()" Title="Built in">
            <Example>
                <BuiltInModals />
            </Example>
        </CodeSnippet>

    </DocsExample>
</Page>
