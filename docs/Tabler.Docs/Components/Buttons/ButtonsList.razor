<ButtonList>
    @for (int i = 0; i < 20; i++)
    {
        var count = i;
        <Button>Button @count</Button>
    }
</ButtonList>

<div class="mt-4">Centered</div>
<ButtonList Centered>
    <Button>Cancel</Button>
    <Button BackgroundColor="TablerColor.Primary">Save</Button>
</ButtonList>
