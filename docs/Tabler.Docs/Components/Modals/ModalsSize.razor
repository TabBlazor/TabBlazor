<ItemSelect Items="EnumInfo.GetList<ModalSize>()" @bind-SelectedValue="@modalOptions.Size" TextExpression="e=> e.ToString()" ValueExpression="e=> e.ToString()" />
<Button @onclick="ShowModal" BackgroundColor="TablerColor.Primary">Show Modal</Button>

<div>@message</div>


@code {
    @inject IModalService ModalService
    private string message;
    private ModalOptions modalOptions = new ModalOptions();

    protected async Task ShowModal()
    {
        message = string.Empty;
        var result = await ModalService.Show("This is a Title", typeof(TestModalContent), null, modalOptions);

        if (result.Cancelled)
        {
            message = "Modal was cancelled!";
        }
        else
        {
            message = "Modal as ok!";
        }

    }

}