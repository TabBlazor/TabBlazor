@page "/docs/progress"

    <Page>
        <DocsExample Title="Progress" ComponentType="typeof(Progress)">

            <CodeSnippet ClassName="@typeof(ProgressesBasic).ToString()" Title="Basic">
                <Example>
                    <ProgressesBasic />
                </Example>
            </CodeSnippet>

            <CodeSnippet ClassName="@typeof(ProgressesSize).ToString()" Title="Size">
                
                <Example>
                    <ProgressesSize />
                </Example>
            </CodeSnippet>

            <CodeSnippet ClassName="@typeof(ProgressesIndeterminate).ToString()" Title="Indeterminate">
                <Description>
                    Waiting....
                </Description>
                <Example>
                    <ProgressesIndeterminate />
                </Example>
            </CodeSnippet>

        </DocsExample>
    </Page>
   
